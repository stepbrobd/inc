# Package & Scope auto discovery/creation/override (with self overlay injection)

See implementations in ../lib/import-packages-tree.nix (import) and
../lib/local-packages-from.nix (export).

Auto update in ../.github/workflows/bump.yaml (opt in with `passthru.autobump`,
default to false).
